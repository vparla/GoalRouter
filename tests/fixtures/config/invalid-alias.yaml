# SPDX-License-Identifier: MIT
# File: tests/fixtures/config/invalid-alias.yaml
# Purpose: Structurally valid configuration with an unknown model alias

schema-version: 1
default-task: bounded-diagnosis
planner-task: bounded-diagnosis
completion-task: bounded-diagnosis
maximum-read-concurrency: 1
repository-inspection-timeout-seconds: 120
model-aliases:
  economy:
    model: example-economy
    reasoning-effort: low
    rank: 10
tasks:
  bounded-diagnosis:
    description: Diagnose bounded work.
    model-alias: missing
    sandbox: read-only
    approval: automatic
    timeout-seconds: 60
    max-attempts: 1
matching: []
hard-risk-rules: []
